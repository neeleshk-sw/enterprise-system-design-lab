# Active inside docker-compose (SPRING_PROFILES_ACTIVE=docker).
spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://postgres:5432/multimodule}
    username: ${DB_USERNAME:multimodule}
    password: ${DB_PASSWORD:multimodule}
