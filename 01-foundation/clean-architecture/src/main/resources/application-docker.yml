# Active inside docker-compose (SPRING_PROFILES_ACTIVE=docker).
spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://postgres:5432/cleanarch}
    username: ${DB_USERNAME:cleanarch}
    password: ${DB_PASSWORD:cleanarch}
