<?xml version="1.0" encoding="UTF-8"?>
<!-- Structured, trace-correlated console logging. The [trace=...] field is
     populated from the MDC by RequestLoggingFilter. JSON output (for log
     aggregation) is introduced in the observability section. -->
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level [trace=%X{traceId:-}] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
